import sys
import math
import string


# Always start with reading in the number
#of test cases gtom standard input. The rstricp() methof removes the lingerin newline
cases = int(sys.stdin.readline().rstrip())

for caseNum in range(cases):
    
    
    dataString = sys.stdin.readline()
    data = dataString.split(" ")
    
   
    shipyardDataLength = int(data[0])
    shipDataLength = float(data[1])
    
    
    shipyardData = []
    
    
    for i in range(len(shipyardDataLength)):
        shipyardData[x] = sys.stdin.readline()
    for i in range(len(shipyardDataLength)):
        shipData[i] = sys/stdin.readline()
